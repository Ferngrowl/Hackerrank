using System;

class HackerRankSolutions
{
    // Sum of an array of integers
    public static int simpleArraySum(List<int> ar)
    {
        int sum = 0;
        foreach (int num in ar)
        {
            sum += num;
        }
        return sum;
    }
    
    
    // Main method to test the solutions
    public static void Main()
    {
        // Call specific methods to test the solutions
        
    }
}
